%h1 Create Post
= form_with(model: @post, url: post_create_path(params[:user_id])) do |form|
  .field
    = form.label :msg
    = form.text_field :msg
    = form.hidden_field :user_id
  .actions
    = form.submit "Create"